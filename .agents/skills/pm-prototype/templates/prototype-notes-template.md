# Prototype notes — <feature> (handoff payload for pm-plan-and-estimate)
**Produced by `pm-prototype`, <date>.** Companion to `<feature>-prototype.html`.

## Hypothesis
<The riskiest assumption, restated as testable — and what observable behavior validates it.>

## Chosen direction (and rejected alternatives)
- **Chosen:** <direction> — <one sentence why it most directly tests the hypothesis>.
- Rejected: <direction B> — <why not>.
- Rejected: <direction C> — <why not>.

## Persona / context
<Who uses this, their technical level, what their workflow looks like today.>

## Assumptions (carried into planning)
- [Assumption] <each assumption the prototype bakes in — these feed WF3's RAD>.

## Open questions
- <Design/product questions surfaced while building — including anything shown on the
  prototype's real-vs-faked panel>.

## Edge cases: wired vs. deferred
- **Wired:** <which empty/loading/error/lock states actually work on screen>.
- **Deferred:** <what was not built (e.g. admin/governance surfaces per the scope
  rule) — name it so WF3 plans it instead of rediscovering it>.
